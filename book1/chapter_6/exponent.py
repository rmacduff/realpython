from __future__ import print_function

base = raw_input("Enter a base: ")
exponent = raw_input("Enter an exponent: ")
print("{} to the power of {} = {}".format(base, exponent, float(base)**float(exponent)))
